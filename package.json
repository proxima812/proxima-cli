{
  "name": "ui_proxima",
  "version": "1.0.0",
  "description": "CLI for bootstrapping Proxima UI in Astro projects",
  "type": "module",
  "bin": {
    "ui_proxima": "./dist/bin/ui_proxima.js"
  },
  "files": [
    "dist",
    "registry"
  ],
  "keywords": [
    "astro",
    "cli",
    "ui"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "npm run build",
    "release": "npm version patch && git push && git push --tags",
    "release:minor": "npm version minor && git push && git push --tags",
    "release:major": "npm version major && git push && git push --tags"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
